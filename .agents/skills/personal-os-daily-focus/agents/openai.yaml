interface:
  display_name: "Personal OS Daily Focus"
  short_description: "Choose a focused, realistic plan for today"
  default_prompt: "Use $personal-os-daily-focus to plan my highest-leverage work today."
dependencies:
  tools:
    - type: "mcp"
      value: "personal_os"
      description: "Local Personal OS data and workflow tools"
policy:
  allow_implicit_invocation: true
